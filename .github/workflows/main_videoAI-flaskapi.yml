# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - videoAI-flaskapi

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://conainervideoai.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_fff205c741364edb9c1020bd8ba59bd3 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_0c79308c903f4a27a1a770ed25f9cd4e }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: conainervideoai.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_fff205c741364edb9c1020bd8ba59bd3 }}/flaskapp:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'videoAI-flaskapi'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_54e27a6611324236966aa20acc1147ff }}
        images: 'conainervideoai.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_fff205c741364edb9c1020bd8ba59bd3 }}/flaskapp:${{ github.sha }}'